<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= code %></title>
  <meta charset="utf-8">
</head>
<body>
  <header header style="padding: 20px">
    <% include ./partials/_header %>
  </header>
  <tr>
    <th><h2>Ooops something went wrong</h2></th>
  <tr>
    <td><%= message %></td>
    <% if(code === 401) {%>
      <td>Please <a href="/login">sign in</a>.</td>
    <% } else {%>
      <td>click <a href="/urls">here</a> to view your available links.</td>
    <% } %>
  </tr>
</body>
</html>
